version: '3.1'

services:
  nginx:
    image: nginx
    restart: always
    hostname: nginx
    container_name: nginx
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./appdata/nginx.conf:/etc/nginx/nginx.conf
      - ./appdata/conf.d:/etc/nginx/conf.d
      - ./appdata/cert:/etc/nginx/cert
      - ./appdata/html:/usr/share/nginx/html
      - ./appdata/log:/var/log/nginx
    environment:
      - TZ=Asia/Shanghai